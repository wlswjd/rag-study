day7_mini/
├── recipes_data.py      # 원본 데이터 (15개 레시피)
├── indexer.py           # 인덱싱 (한 번만 실행)
├── searcher.py          # 검색 모듈 (재사용 가능)
├── main.py              # 실행 예시
├── verify.py            # 인덱스 검증
├── mini_chroma_db/      # Dense 인덱스 (자동 생성)
└── bm25_index.pkl       # BM25 인덱스 (자동 생성)